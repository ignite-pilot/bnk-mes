# Server
PORT=3000
NODE_ENV=development

# ig-member 연동 (해당 서비스 기동 URL)
MEMBER_API_BASE_URL=http://localhost:8080
# ig-member 인증 경로 prefix (기본: /api/auth → /api/auth/login, /api/auth/me 등)
# MEMBER_AUTH_PATH_PREFIX=/api/auth

# MySQL (AWS Secret Manager prod/ignite-pilot/mysql-realpilot 참고)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bnk_mes
